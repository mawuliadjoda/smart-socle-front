/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RecouvrementAssuranceCService } from './recouvrement-assurance-c.service';

describe('Service: RecouvrementAssuranceC', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RecouvrementAssuranceCService]
    });
  });

  it('should ...', inject([RecouvrementAssuranceCService], (service: RecouvrementAssuranceCService) => {
    expect(service).toBeTruthy();
  }));
});
