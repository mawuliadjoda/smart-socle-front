/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { RefMedicamentAnsmService } from './refMedicament-ansm.service';

describe('Service: RefMedicamentAnsm', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RefMedicamentAnsmService]
    });
  });

  it('should ...', inject([RefMedicamentAnsmService], (service: RefMedicamentAnsmService) => {
    expect(service).toBeTruthy();
  }));
});
